# без проверок установим и запустим докер
- name: Install Docker
  ansible.builtin.package:
    name: docker.io
    state: present

- name: Ensure Docker service is running
  ansible.builtin.service:
    name: docker
    state: started
    enabled: yes
